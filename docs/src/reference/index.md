# 参考

这里放的是“查表型”内容：

- provider 能力矩阵（哪些 provider 支持哪些能力、对应 feature flags）
- `ditto-gateway` 的 CLI 选项速查
- FAQ / 术语表

如果你更需要“教程式”的路径，请优先阅读 SDK/Gateway 两章。

历史代码审查页属于归档材料：

- 会继续保留在 mdBook 导航里，方便追溯设计演进
- 不会进入 `llms.txt` 的自动聚合，避免把历史 owner 路径和已过时结论回灌给 LLM 入口
