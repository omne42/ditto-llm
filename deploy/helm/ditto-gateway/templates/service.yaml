apiVersion: v1
kind: Service
metadata:
  name: {{ include "ditto-gateway.fullname" . }}
  labels:
    {{- include "ditto-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ditto-gateway.selectorLabels" . | nindent 4 }}
